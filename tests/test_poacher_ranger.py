

def test_ranger_catches_poacher():
   ''' board = Board(10, 10)
    capital = Capital(1000)
    ai = WildlifeAI(board, capital)

    board.spawn_ranger(Vector2(5, 5))
    board.spawn_poacher(Vector2(6, 5))

    ai.update(1.0)
    assert len(board.poachers) == 0
    assert capital.current_balance == 1050'''
   pass
